- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.ideavimrc: ideavimrc

- shell:
    - [git submodule update --init --recursive, Installing Submodules]
